// Entry point. Rendering (src/engine, src/world) is not implemented yet in
// this scaffold — see CLAUDE.md. This file exists so `vite build` has a
// bundle root.
const app = document.querySelector<HTMLDivElement>("#app");
if (app) {
  app.textContent = "Five Safes Archipelago — scaffold, no renderer yet.";
}
